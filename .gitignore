# See https://www.dartlang.org/guides/libraries/private-files

# Files and directories created by pub
.dart_tool/
.packages
build/
# If you're building an application, you may want to check-in your pubspec.lock
pubspec.lock
!ip/pubspec.lock

# Directory created by dartdoc
# If you don't generate documentation locally you can remove this line.
doc/api/

# dotenv environment variables file
.env*
!.envrc

# Avoid committing generated Javascript files:
*.dart.js
# Produced by the --dump-info flag.
*.info.json
# When generated by dart2js. Don't specify *.js if your
# project includes source files written in JavaScript.
*.js
*.js_
*.js.deps
*.js.map

.flutter-plugins
.flutter-plugins-dependencies

# Nix build result symlink
result
.direnv

# Dart tool cache under ip/
ip/.dart_tool/

# Model weight cache (large binary files)
.cache/

# Generated SoC / synthesis output dirs (RTL, bitstreams, nextpnr JSON)
out/
out_uart/
build_usb/
build_uart/
*.bit
*.config

# Zig runtime build artifacts
runtime/zig-out/
runtime/.zig-cache/

# Python bytecode
__pycache__/
*.pyc

# Genip model output dirs (loom.json + scales/glue/weights/bitstream)
out_*/
